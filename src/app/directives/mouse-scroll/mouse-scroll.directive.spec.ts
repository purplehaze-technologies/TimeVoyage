import { MouseScrollDirective } from './mouse-scroll.directive';

describe('MouseScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseScrollDirective();
    expect(directive).toBeTruthy();
  });
});
